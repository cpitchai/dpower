apiVersion: v1
kind: Service
metadata:
  name: {{ include "datapowerservice.fullname" . }}
  labels:
    {{- include "datapowerservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datapowerservice.selectorLabels" . | nindent 4 }}
