apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datapowerservice.fullname" . }}-test-connection"
  labels:
    {{- include "datapowerservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datapowerservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
